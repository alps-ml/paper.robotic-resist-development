# Requirements for the venv in this ROS workspace
# Run 
# > python3 -m venv venv 
# > source ./venv/bin/activate
# > pip install -r requirements.txt
# to install the requirements
# Note: The requirements are not pinned to specific versions to allow for flexibility
# in the development process. However, it is recommended to use a virtual environment
# to avoid conflicts with system packages.

catkin_pkg
lark
empy
pyyaml
trimesh
pytest
mecademicpy
